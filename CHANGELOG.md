# 更新日志

记录使用者能感知的变化：支持的 DSH 版本、功能、修复、配置迁移和已知限制。版本号与 `package.json` 一致。Alpha 阶段仍可能出现不兼容变更，这类变更单独列出并说明处理方法。

DSH 用 pnpm 安装插件，重新安装同一版本号的打包文件不会替换已安装的代码。每次更新都使用新版本号的打包文件，安装后重启 DSH。

## 0.1.0-alpha.6（未发布）

适配 DSH `0.1.7-rc.2`，支持范围为 `>=0.1.7-rc.2 <0.1.8-0`。`0.1.0-alpha.4` 和 `0.1.0-alpha.5` 是开发分支上的中间版本，内容已并入本版本。

### 不兼容变更

- 不再支持 DSH `0.1.6`。请先升级 DSH，再安装本版本并重启。
- 配置从 DSH home 的 `settings.yaml` 改为保存在当前 profile 的 `cordis.patch.yml`。首次启动时会自动迁移名称、头像、模型分组和提示词，已保存的新配置不会被覆盖。DSH 日志中关于 `dsh-persona-avatar` 未导入的提示属于预期情况。迁移条件见[从 DSH 0.1.6 升级](docs/INTEGRATION.md#从-dsh-016-升级)。

### 变更

- 聊天中每轮回答只显示一个 AI 头像，位于这一轮最上方的可见行，通常是"深度求索中"状态行。思考过程、回复正文和操作栏与名称对齐。此前每个 assistant 步骤各显示一个头像。
- 宿主拒绝保存时，配置页提示原因并保留修改，可重试或重新载入配置。

### 相对开发版的修复

以下问题只出现在开发分支的 `alpha.4` 和 `alpha.5` 中：

- DSH `0.1.7` 中两个组件配置页空白。
- 模型思考期间不显示 AI 头像，要等回复正文出现才显示。

### 已知限制

- 模型不变的后续轮次，AI 头像在第一个思考或回复片段出现时显示；DSH 只在请求配置变化时记录新的模型信息。
- 尚未在真实 DSH 中验证工具调用、steering 轮次、真实 DeepSeek 接口的流式回复，以及从 `0.1.6` 升级的完整迁移。当前验证范围见[验证状态](docs/INTEGRATION.md#验证状态)。

### 开发

- 新增[模拟 DeepSeek 接口](scripts/mock-deepseek.mjs)和[真实宿主聊天检查](docs/DEVELOPMENT.md#真实宿主聊天检查)，用于在 DSH 升级后确认聊天头像。

## 0.1.0-alpha.3（2026-09-23）

首个公开版本，适配 DSH `0.1.6-alpha.2`。

- 设置用户名称与头像；创建多套 AI Persona，每套包含名称、头像和多个精确的渠道/模型关联。每个模型最多属于一套 Persona，移动已有归属需要确认。
- 聊天消息按实际请求模型显示对应 Persona 的名称和头像，未关联的模型保留宿主显示。
- 提示词组件：为 Persona 组或原生模型追加字面系统提示词，按列表顺序生效，遵守 Agent 完整提示词预设。
- 配置自动保存，携带修订号防止覆盖，失败或冲突时保留输入。
- 配置页内的消息预览，支持暂停、重播、进度拖动和倍速。
- 插件详情页与组件配置页的品牌视觉和背景动效。
